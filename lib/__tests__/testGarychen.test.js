const assert = require('assert');
const testGarychen = require('../index.js');

describe('testGarychen', () => {
  it('has a test', () => {
    assert(false, 'testGarychen should have a test');
  });
});
